pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "Clean_KMM_Application"
include(":androidApp")
include(":shared")